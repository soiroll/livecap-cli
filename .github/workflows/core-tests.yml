name: Core Tests

on:
  push:
    branches:
      - main
  pull_request:
  workflow_dispatch:

jobs:
  tests:
    runs-on: ubuntu-latest
    strategy:
      fail-fast: false
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}

      - name: Set up uv
        uses: astral-sh/setup-uv@v3

      - name: Sync dependencies
        run: uv sync --extra translation --extra dev

      - name: Run tests
        run: uv run python -m pytest tests

  optional-extras:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        extra:
          - engines-torch
          - engines-nemo
    steps:
      - name: Check out repository
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Set up uv
        uses: astral-sh/setup-uv@v3

      - name: Sync dependencies
        run: uv sync --extra translation --extra dev --extra ${{ matrix.extra }}

      - name: Smoke import
        run: |
          uv run python - <<'PY'
          import importlib
          import livecap_core

          optional_module = {
              "engines-torch": "engines.parakeet_engine",
              "engines-nemo": "engines.reazonspeech_engine",
          }["${{ matrix.extra }}"]
          importlib.import_module(optional_module)
          print("ok")
          PY
